# 菜单声音设计

当前采用布料软接触的短促拟音：带一点闷感和轻微纤维摩擦，作为按钮被轻轻按下的反馈。以用户提出的羊毛放置感为质地参考。

五个菜单角色使用 lipalearning 的布团落在软垫、地毯上的实录。确认稍实（190 ms / -27 dBFS），返回略低、略轻（180 ms / -29 dBFS）；切换和微调更短更轻（105 / 85 ms，-33 / -35 dBFS）；首次入场保留一次轻触（230 ms / -29 dBFS）。均保留原录音音高和接触质地，只去掉接触前空白、调整响度与平滑剪辑边缘。

沿用 MenuAudioService 的预加载固定播放器与 UI 总线。音频离线剪辑，不在运行时合成、滤波或创建播放器。Ambient1 保持原样；校准时菜单音静音，正式游玩不播放菜单环境音。古琴、木鱼候选均退出默认播放。

各角色提供三种不同布料接触实录，包含已认可的原声及两个相近变体，按相同 RMS 与时长制作。每次等权随机选择，并排除上次播放的录音。使用 Godot 自带 AudioStreamRandomizer，继续复用五个播放器；不通过随机变调制造变化。

声音只承担交互反馈，保持克制。具体听感由用户试听与实际游玩确定，技术数值不能代替听感验证。制作工具、来源与参数见 docs/menu-audio.md 及 tools/audio/sources/README.md。

Ambient1 在工作室开屏结束、标题画面开始显现时开始播放，早于“点击任意处开始”提示；点击只触发短入场声，环境音连续播放。
