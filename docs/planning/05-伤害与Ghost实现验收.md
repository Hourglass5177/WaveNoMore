# 伤害拆分、Ghost 预测与起手提示

2026-09-14，`editor` 工程内实现；未 build、提交或推送。参数入口及具体语义见 [策划数值](README.md)。

## 运行链路

- Tap 使用 `tap_miss_damage`，未命中抵达角色时结算。
- `NoteJudgeEngine` 记录失败 Hold 的未消耗起始时间；`GameplaySimulation` 按 TempoMap 生成剩余段的微秒结算点。每段伤害为 `hold_segment_damage`，单位拍长为 `hold_damage_segment_beats`。身体不足整段按累计比例取整。
- Hold 头部抵达不单独扣血，失败当刻也不扣整条。离圈曲线保留实际姿态，抵达和剩余身体裁短读取同一领域时间线。Tap 与 Hold 的伤害组分开，同组 Hold 按段去重。
- `GhostWaveForecast` 在装载时准备理想持有和调频采样事件。到关联路径预告时刻，复制真实可见波与源相位，只在副本中重演未来区间并求交。预测只执行一次，后续输入不移动目标。
- `TuningEngine.ghost_grade` 按关联单程当前应完成行程或端点结果判断；双方关联取共同成功。复用端点捕获余量，当前为 Perfect／Miss。位置不参与判定。失败按已编排枚数乘 `ghost_miss_damage` 逐枚结算。
- `ChartPathAdapter` 保留新版 Ghost 的路径关联，映射到目标拍覆盖的单程；JSON 不增加字段。关联 ID 进入编译内容摘要，新规则参数进入规则摘要。
- 起手缩圈取消与条身相交部分的裁切，完整画在轨道上层；轨道端帽本身仍只有外轮廓，不重复叠圆。

自由跳时仍按原功能建立没有过去输入的新时间线；完整预览定位与 Replay 从头重演，恢复原预测暂态。生成不依赖画面请求，也不会每帧扫描整曲历史求交。

## 验证

| 检查 | 结果 |
| --- | --- |
| 分段伤害 | 25 项通过：抵达微秒边界、变 BPM、断持和续接、暂停、重试、30/60/120 及不规则帧步 |
| Ghost 新预测与关联判定 | 29 项通过：v2 双侧关联、未来改频、源状态隔离、固定位置、不同帧步、生成问题不扣血 |
| Ghost 候选查询 | 720 组原对照＋252 组边界和密集对照通过；未改变原算法坐标及顺序 |
| 策划参数 | 63 项通过：实际 XLSX、40 项字段、规则副本、真实判定与正式预览装配 |
| 表格同步保值 | 临时将 Tap 改为 17、Hold 单位拍长改为 0.125，同步并重开后保留；交付表仍为 20／0.25 |
| 随从及正式关卡伤害 | 262 项通过，包含 Compatibility 实际渲染、HUD 扣血、减伤取整及重试 |
| 起手缩圈与滑条 | 43 项实际渲染检查通过：内侧完整像素、预填、圆帽接合、两侧颜色与不同半径 |
| 预览恢复 | 原有暂停、后台冻结及定位测试通过 |

旧领域总套件仍有 11 项、旧调频圆弧套件有 24 项失败，主要使用旧自由调频／提前完成规则。本轮没有为通过这些旧用例恢复旧规则。将模拟器替换为当前提交中的旧版本，领域套件同样得到这 11 项失败。

示例谱 `tests/editor/fixtures/tuning` 的 `ghost_60` 指定 3 枚，理想预测只有 2 个满足间距的交点，写谱器会报告不足；保持正确调频不因此扣血。该谱面未被自动改写。Ghost 仍未新增独立计分单位，滑条评分链路保持原状。

截图：`builds/visual-review/tuning-style/start-ring-0.png`、`start-ring-1.png` 及该目录 `preview-*.png`；正式伤害截图沿用 `builds/pet-review/` 输出。测试记录位于 `builds/planning/`。
