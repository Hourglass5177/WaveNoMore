# UI、editor 与 develop 同步审查

2026-09-14。本次整理、提交并合并三个分支，不重新构建发布程序。

## 来源与合并原则

- 原 editor 基线 `54d78b5`；本地编辑器、动画及策划表成果先保存为 `d51f3e4`。
- 新暂停、设置、随从 UI 与交互动效先保存为 `6291512`。
- 队友远端 ui 的新增提交为 `b3a2cd2`、`d9e7fc3`，包含选关卡片、标题和火框。远端 ui 当时尚未包含最新 editor 的两个提交，不能仅以最后一次 Git 合并成功判断已经同步。
- 采用保留父提交的合并；不重写远端历史。最终三个分支统一到同一提交。

## 冲突处理与修正

1. 保留 editor 的调频条透明度、完整起点缩圈、计时提示和网格优化，同时接入 UI 字体。领域逻辑、运行链路及两个编辑器实现与 editor 保存提交一致。
2. 背景编辑器的图层名称与横向随机重复配置字段均保留，历史签名包含两边新增字段，避免撤销或保存丢字段。
3. 随从素材、正式播放器、技能特效和 UID 全部使用 editor 最新版本，替换 UI 初期快照；菜单调用 `trigger()` 的接受结果控制投影动效。保留菜单自己的尺寸、投影色、隐藏说明和装备操作。
4. 保留队友选关卡片、标题及火框参数；火框修复长帧丢时间问题，补齐素材帧并保留余量。没有动画的卡片停止帧更新。
5. 选关背景纳入 Design 画布等比缩放，非设计分辨率下仍居中；保留卡片向下 69 px 的原稿位置。
6. 两份相同 Huiwen 字体统一为 `assets/fonts/huiwen.otf`，关卡卡片与新 UI 共用。临时 Excel 检查输出不入 Git。
7. 测试同步新随从页接口、卡片构图及分辨率设置；临时文件由测试自己创建目录，干净检出也可运行。

## 验证

Godot 4.7.2，Compatibility。使用独立 `WaveNoMore-UIReview` 用户目录；结束删除临时 override，未改变玩家设置和存档。

| 检查 | 结果 |
| --- | --- |
| 编辑器导入与解析 | 通过，无脚本或导入错误 |
| 实际 XLSX、策划规则及默认值一致性 | 85 项通过 |
| 双 Hold／调频、外部时钟合并回归 | 通过 |
| 随从正式运行、重试及形态 | 161 项通过 |
| 随从技能、装备、保存与菜单 | 262 项通过 |
| 角色行走与位置适配 | 29 项通过 |
| 分界线声波及场景素材保存 | 89 项通过 |
| 写谱器预览帧、定位运动缓存 | 两套通过 |
| 关卡编辑器动画工作流、可靠性 | 两套通过 |
| 选关轮播、长帧火框、缩放与构图 | 388 项通过 |
| 调频条真实图形检查 | 43 项通过 |
| 新 UI、暂停、设置与随从交互 | 103 项通过 |
| 手柄目录滚动、弹窗焦点与返回 | 51 项通过 |
| 五档分辨率、正式字体及编辑器隔离 | 31 + 1 项通过 |

最初测试发现旧菜单接口和缺失临时目录，并因测试未跟随原稿 69 px 偏移而失败；修正后分别复跑，最终以上结果均为通过。测试与截图位于本地 `builds/merge-review`、`builds/ui-review` 和 `builds/level-carousel`。

本次保留队友独立 `level_choosing.tscn` 的开发范围；正式歌曲目录继续使用现有入口，未擅自为尚未配置 stage_id 的卡片编造关卡关联。新暂停、设置和随从已接正式应用入口。
