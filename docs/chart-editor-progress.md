# 写谱器开发与验证记录

基线：develop@9c0dded；分支：editor；Godot：4.7.2.stable.official.ed1daf0bf。记录日期：2026-09-07。

当前可以运行和交付开发版体验包，尚未完成设计说明书的全部验收。调频、素音没有在新版中实现；正式游戏的输入和判定规则没有为了预览测试而调整。

界面已完成一轮文案、SVG 图标、分组与状态同步整理；新增 UI 回归后共四组新版测试。尺寸、缩放及验证边界见 [界面调整记录](chart-editor-ui-review.md)。

## 分阶段结果

| 阶段 | 已实现、已检查 | 剩余事项 |
|---|---|---|
| A 基线 | editor 分支；修复废弃输入测试接线；独立启动分流；禁用玩家设置/存档启动副作用；带超时和日志判错的测试入口 | 领域测试仍有下述既有失败 |
| B 预览与音频 | 外部语义批次；正式理想输入；统一 Transport；无声重演与最新定位取消旧请求；中段 Hold、接触反馈、波前恢复；WAV/OGG/MP3 独立解码波形；五档保音高测量 | 所有效果的时间恢复尚未逐项验证；音频瞬态、循环尾音和端到端同步未通过完整验收；密集谱重演偏慢 |
| C 最小闭环 | 共享 JSON v1/ZIP 加载；类型化文档、未知字段保留；歌曲与难度历史；音乐导入、点击 Tap、拖画 Hold、选择/端点、保存重开；未保存修改预览 | 复杂异常文件和中断保存情境需扩展实际文件操作验证 |
| D 工作区 | 静态场景分区；双轨、秒尺/节拍尺、波形；多选、框选、移动/跨侧、复制/重复、量化、属性、组合、撤销；F/J 步进、焦点隔离；BPM/拍号、段落、循环、缩放、多难度复制、主题/配色 | 完整谱师人工操作验收；所有菜单与快捷键等价性复核；密集谱实际渲染交互测量；外观选择目前为稳定键文本输入 |
| E 交付 | 恢复稿、另存为、音频重定位；旧路径关卡转换与逐字节依赖归档/报告；按难度 ZIP、游戏共享加载；匹配 4.7.2 Windows 模板与独立导出配置 | 无 Godot 的另一台电脑上完整制谱验收；异常退出恢复场景人工复核；新版验收后删除旧工具 |

## 可重复验证

在 Game 目录运行 `tests/editor/run.ps1 -GodotExe <引擎路径>`，每个套件独立进程，默认 60 秒超时，同时检查完成标记、退出码和错误日志。

- `run_studio_tests.gd`：JSON 时间/往返、未知内容、结构错误、草稿冲突；实际鼠标事件拖画、选区移动、头尾把手、Shift 框选、一次撤销；组合复制；保存重开、三种音频波形、旧谱归档、另存为、ZIP 与游戏读取；Hold 状态/波前恢复与取消过期请求。
- `run_input_tests.gd`：通过 Godot 输入分发验证空格、F/J、文本焦点；未按回车的 135.125 BPM 在点击保存时提交，且一次撤销。
- `run_workspace_smoke.gd`：实例化实际工作区、打开示例、定位 4.2 秒。带渲染运行时另存截图。
- 对 2.02、2.3、3.05、4.2、5.3、6.2 秒，比较从头推进与直接定位的活动音符 ID、接触、时刻、Hold 进度、位置和颜色。当前通过；这不等于已验证所有主题中的每一种特效。

领域测试已恢复可编译，保留 7 项基线失败：核心 131 项中 1 项（暂停重按 Hold，预期 0 实际 3）；调频 101 项中 6 项（提前完成与末段回退、最佳等级/进入标记/误差时间）。共享索引及预览改动后重复执行，失败集合未扩大。没有将全工程测试描述为全绿，也没有恢复已废弃的 InputRouter。

## 性能记录

环境：Windows、本机 Intel Core Ultra 9 185H、Godot 4.7.2，headless。测试构造 5 分钟、10000 个交替侧 Tap，保留完整模拟，不跳过音符。原始结果见 [性能数据](chart-editor-performance.json)。

| 项目 | 毫秒 |
|---|---:|
| 一次插入 10000 音符命令 | 142.598 |
| 装配/编译预览 | 1541.312 |
| 定位 0 秒 | 399.852 |
| 定位 150 秒 | 14530.273 |
| 定位 299 秒 | 40028.507 |
| 299 → 299.016 秒普通推进一次 | 20.791 |

已根据测量优化全谱冲突扫描、判定活动窗口、到达事件索引和批次快照发布。完整重建每约 8 毫秒让出主线程，可被新请求取消，但 40 秒等待仍不能作为满意的制谱体验。下一步应剖析完整恢复热点，再决定稀疏检查点；不把当前数字隐藏为“后台完成”。以上不包含真实 GPU 渲染下的帧率和鼠标反馈验收。

## 音频证据与限制

五档 .5/.75/1/1.25/1.5 倍，独立音乐总线使用逆向 PitchShift（FFT 1024、oversampling 4）。真实 WASAPI 驱动下录制总线，440 Hz 稳态频率五档均测得 440 Hz；数据见 [音频测量](chart-editor-audio-measurement.json)。

1 倍首音额外延迟约 1.42 ms，其余档约 12.04–12.79 ms；差值约 11 ms。Transport 加入 512 / mix_rate 的处理延迟估计，同时独立保存设备试听补偿。该补偿尚需与实际输出、提示音和屏幕反馈联合复测。

总线录音不证明扬声器端到端延迟；纯音频率不证明音乐瞬态质量。当前提示音仍通过运行帧触发；循环尾音、变速切换后的残留缓冲及说明书要求的 ≤10 ms 同步尚未完成验收。不能把五档“可播放”写成保音高方案全面通过，也不以随速率变调降级替代。

五分钟 1 倍实测：WASAPI、48000 Hz 总线录音，30 个脉冲的首尾理论间隔 290 秒，实测 290.000958 秒（误差 0.958 ms）；Transport 相对墙钟的前后区间中位数变化 0.698 ms。该单倍率测量通过 5 ms 漂移标准，其他倍率及真实输出仍未覆盖。原始摘要见 [长期音频测量](chart-editor-audio-longterm.json)，复测使用 `measure_audio_longterm.gd` 与 `analyze_longterm_audio.py`；长测不要用按帧数提前退出的 `--quit-after`。

## 交付与协作边界

- 公共 JSON/领域/预览接入口与工具 UI 分开提交；共同接口见 [接口说明](chart-editor-interfaces.md)。
- `builds/chart-studio/` 为本地生成物，不入 Git。发布预设仅导出写谱器，正式游戏没有另行导出。
- 当前 EXE 已在工程目录之外以嵌入 PCK 启动，日志无脚本错误；实际渲染工作区截图检查通过（RTX 4060 Laptop、OpenGL Compatibility）。这两项不替代另一台电脑的完整操作验收。
- 旧资源导入的新工程保留 `legacy/`；可玩包不执行这些内容。编辑稿可保存不受支持的音符，但不能以其为可玩导出。
- 旧编辑器暂留，原因是计划明确要求“新版通过验收后删除”。不新增旧工具功能，不将双实现作为长期安排。
- 后续优先完成音频与密集谱验收，再进行全流程谱师试用和旧工具清理；当前包标注为开发版。


## 2026-09-07 稳定性与实时录入

本轮实现恢复入口统一初始化、当前难度与视图恢复、1 秒恢复稿防抖、独立波形请求编号；颜色以局部表现接口预览、关闭弹窗一次提交。文档区分普通资料、外观、音符、时间映射等变更；普通资料和配色不重编玩法。音频同一播放区间单调推进，主动定位与循环显式恢复。时间线静态绘制与播放头分离，定位拖动合并至 50 ms，松手精确定位。

加入 R 开关的 F/J 实时录入、150 ms 可调真实时长阈值、吸附和整段撤销；新增界面缩放菜单、秒标尺、负时间背景、音乐零线、首拍标注、水平滚动条、循环把手、边缘自动滚动及播放跟随。

验证：五组写谱器测试通过；新增稳定性套件另以实际渲染运行通过。包括非空多难度恢复、波形替换与同路径请求取消、12 次颜色弹窗连续拖色及撤销、五档倍率录入、双键及循环截断、输入事件录制与一次撤销、窗口尺寸缩放、循环把手取消和标尺最终定位。测试恢复稿隔离于用户目录中的 tests 子目录。

RTX 4060 Laptop / Core Ultra 9 185H / Godot 4.7.2 Compatibility：连续 120 帧中位数 5.933 ms，P95 8.190 ms，最大 10.705 ms；重建计数始终不变。12 次弹窗提交、撤销及帧等待合计 209.445 ms，调色未增加关卡装入或恢复次数。原始逐帧、音符生成回收与接触记录在本地 builds/stability/playback-trace.json；截图为同目录 workspace.png。该短样本不是长时间真人谱师验收，也不证明所有反馈动画都已无视觉问题。

5 分钟 10000 音符：插入 127.743 ms，编译 1352.490 ms，定位 0/150/299 秒为 372.500/12897.917/34804.427 ms，普通推进 19.090 ms。记录见 chart-editor-performance.json。批量插入超过 100 ms 目标；晚段定位仍明显慢，热点在正式状态的历史重演，需后续进一步剖析与优化。没有通过丢弃历史事件降低耗时。

用户 builds/chart-studio/测试 工程及找到的恢复备份均为零音符，没有发现可恢复的音符数据；本轮未覆盖该工程。发布包仅选取 EXE、说明及示例文件，不扫描并打包输出目录内的用户工程。Windows 系统级 DPI 切换、另一台电脑、长期密集谱交互尚需人工复核；未将它们记为通过。


## 2026-09-07 高清预览与可调整布局

预览改为 TextureRect 显示独立 SubViewport 纹理，最低 1920×1080，按物理显示宽度继续增加；保持 1920×1080 游戏坐标。根窗口明确采用 canvas_items，工具图标由 20px 提高到 80px，显示尺寸仍为 20 逻辑像素。

时间线最小高度从 250 降至 164 逻辑像素，两轨随可用高度平分，最小每轨 28。侧栏最小宽高降低；视图菜单加入侧栏、时间线显隐、专注预览和恢复默认布局。分区偏移和显隐写入本机工作区与恢复稿，不进入谱面协议。

六组测试通过。布局测试另以 RTX 4060 Laptop / OpenGL Compatibility 实际渲染：验证 150% UI 缩放下预览纹理至少 1080p、紧凑轨道可见、专注预览与退出恢复、布局调整不重新创建关卡，以及工作区显隐恢复。已有布局测试覆盖 1280×720、1440×900、1920×1080 和 125%／150% 程序缩放；这不代替 Windows 系统 DPI 切换验证。截图在本地 builds/layout-review/workspace.png、focus.png。只重新导出独立写谱器，不导出正式游戏。
