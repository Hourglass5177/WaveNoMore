# 冥河写谱器 v0.1.5

2026-09-12 · Windows x86_64

- 新建项目和新增空白难度默认选择场景列表的最后一项，目前为 s08。后续按目录排序自动选择，不写死关卡 ID。
- 场景演出仍默认关闭。打开已有谱面沿用其保存的场景，复制难度保留原配置。
- 写谱器与 `game/minghe.exe` 同步重建，两个 EXE 的文件版本均为 0.1.5.0。

解压整个目录后运行 `minghe-chart-studio.exe`，点击“在游戏中试玩”使用同包配套游戏。沿用 v0.1.4 的识别器、离线模型、许可和示例。

操作方法见 [写谱器使用说明](写谱器使用说明.md)，工程接入方式见 [场景接入说明](docs/chart-studio-scenes.md)。
